kind: Module
name: frontend
description: Angular frontend
type: container
services:
  - name: frontend
    ports:
      - name: http
        containerPort: 80
    ingresses:
      - path: /
        port: http
