kind: Module
name: frontend
description: Angular frontend
type: container
services:
  - name: frontend
    ports:
      - name: http
        containerPort: 80
    ingresses:
      - path: /
        port: http
    env:
      IMAGE_SERVICE_URL: http://image-service.rat-or-cat.svc.cluster.local:3000
